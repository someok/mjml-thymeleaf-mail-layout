{
  "name": "mjml-template",
  "version": "1.0.0",
  "description": "通过 mjml 生成 thymeleaf 模板",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "lint": "eslint src/",
    "eslint": "eslint",
    "start": "gulp",
    "compile": "gulp compile",
    "format": "eslint --fix src/**/*.js",
    "changelog": "git log `git describe --tags --abbrev=0`..HEAD --no-merges --pretty=format:' * %Cblue%s%Creset (%an)' | grep -v 'Merge'"
  },
  "devDependencies": {
    "eslint": "^6.1.0",
    "eslint-config-prettier": "^6.0.0",
    "eslint-plugin-prettier": "^3.1.0",
    "gulp": "^4.0.2",
    "gulp-clean-dest": "^0.2.0",
    "gulp-header": "^2.0.9",
    "gulp-mjml": "^4.0.2",
    "gulp-replace": "^1.0.0",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3"
  }
}
